// FizzBuzz 1–100
var i = 1
for i = 1 to 100 step i = i + 1 do:
    // check 15 first
    if i % 15 == 0 then:
        print "FizzBuzz"
    elseif i % 3 == 0 then:
        print "Fizz"
    elseif i % 5 == 0 then:
        print "Buzz"
    else:
        print i
    end
end
